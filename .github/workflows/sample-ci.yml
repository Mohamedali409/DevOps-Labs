name: sample-ci

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List all files
        run: ls -R

      - name: Print README content
        run: cat README.md
